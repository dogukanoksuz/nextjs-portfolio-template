/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/nextjs-portfolio-template",
    images: {
        unoptimized: true,
    }
};

export default nextConfig;
